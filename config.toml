# 代理服务配置
[[proxies]]
port = 8080
label = "用户服务"
target = "http://localhost:3000"

[[proxies]]
port = 8081
label = "订单服务"
target = "http://localhost:3001"

# 日志配置
[log]
dir = "./logs"
level = "info"
maxSize = 100
maxBackups = 100

# 监控配置
[monitor]
enabled = true
port = 9090
